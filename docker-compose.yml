version: "3.9"

services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"

  api:
    build: .
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    depends_on:
      - redis

  worker:
    build: .
    command: python worker_app.py
    depends_on:
      - redis

  scheduler:
    build: .
    command: python -m core.scheduler.scheduler
    environment:
      - PYTHONUNBUFFERED=1
    depends_on:
      - redis

  # frontend:
  #   build: 
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   ports:
  #     - "5173:5173"
  #   volumes:
  #     # Enable Hot Reloading (Changes in code reflect instantly)
  #     - ./frontend/src:/app/src
  #   environment:
  #     - CHOKIDAR_USEPOLLING=true